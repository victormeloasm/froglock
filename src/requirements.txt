# -------------------------
# Core (required)
# -------------------------
cryptography>=42.0.0      # AES-GCM, HKDF, secure primitives
argon2-cffi>=23.1.0       # Argon2id KDF for password-based keys

# -------------------------
# Hashing
# -------------------------
blake3>=0.4.1             # BLAKE3 (fallback to BLAKE2b if not available)

# -------------------------
# Optional (performance & acceleration; app works without them)
# -------------------------
numpy>=1.26.0             # Faster buffer XOR and secure random ops
gmpy2>=2.1.5              # Optimized big integer math (ECCFrog522PP)

# -------------------------
# Windows-only extras
# -------------------------
pywin32>=306 ; sys_platform == "win32"  # ACLs, secure temp handling
